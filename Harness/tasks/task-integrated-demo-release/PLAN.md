# Integrated demo release

User authorized integrating the supplied repository and deploying it to GitHub for testing.

- Preserve the existing default branch and UI.
- Integrate shared creative contracts and Python Mock snapshots into the frontend.
- GitHub Pages serves an explicitly offline demo; real generation requires a separately hosted backend.
- Fix repository-relative assets, S03 retry contract mismatch and misleading playback controls.
- Verify Python contracts, frontend unit tests, browser flows, production build and deployed URL.
